/**
 * Hapana Booking Widget — Shared Configuration
 *
 * Production should set NEXT_PUBLIC_HAPANA_WIDGET_ID via env.
 * This fallback exists only so local dev / preview builds without
 * the env var still render the widget. Do not rely on it in prod.
 */

export const HAPANA_WIDGET_ID_FALLBACK = 'ZjV0eGJ3cGUzU1c1VGtlcUxGOVFDUT09';
